# 🎰 Секка - Онлайн карточная игра

Строгая реализация игры Секка (21 очко) с поддержкой:
- Реального времени через Pusher
- Системы комбинаций (СЕКА ТУЗОВ, 32, 31 и т.д.)
- Режима Вары при ничьей
- Системы голосования для новых раундов
- Полной логики ва-банка (All-in)

## 📋 Требования

- **Node.js** версии 14 или выше ([скачать](https://nodejs.org/))
- **npm** (устанавливается вместе с Node.js)
- **Python 3.x** (опционально, для HTTP сервера) ([скачать](https://www.python.org/))

## 🚀 Быстрый старт

### 🎯 Полноценный запуск (рекомендуется)
```bash
FULL-START.bat
```
**Автоматически:**
- Проверяет и устанавливает зависимости
- Запускает сервер (порт 3006)
- Запускает клиент React (порт 3000)
- Открывает приложение в браузере

> 📖 Подробная инструкция: [`КАК-ЗАПУСТИТЬ.txt`](КАК-ЗАПУСТИТЬ.txt)  
> 📋 Все BAT-файлы: [`BAT-ФАЙЛЫ.txt`](BAT-ФАЙЛЫ.txt)

### Альтернативные способы запуска

**Быстрый запуск (если зависимости уже установлены):**
```bash
START.bat
```

**Только сервер:**
```bash
START-SERVER.bat
```

**Остановка всех сервисов:**
```bash
STOP.bat
```

## 📁 Структура проекта

- `server/` - Backend (Node.js + Express + Pusher)
- `client/` - Frontend (React)
- `client/public/game-table-test.html` - HTML интерфейс игры

## 🔧 Технологии

- **Backend:** Node.js, Express, Pusher
- **Frontend:** React
- **Game Logic:** SekaCombinations для определения комбинаций

## 📝 API

Сервер работает на порту **3006** (по умолчанию)
- API: `http://localhost:3006/api`

## 🔒 Безопасность

### ⚠️ Production Ready

Проект настроен для production деплоя. **Все секретные данные ОБЯЗАТЕЛЬНО должны быть настроены через переменные окружения!**

Приложение **не запустится** без правильной настройки переменных окружения.

### Переменные окружения

Проект использует переменные окружения для хранения секретных данных. **НЕ коммитьте** файлы `.env` в Git!

#### Для локальной разработки:
Создайте файл `.env` в корне проекта со следующим содержимым:
```env
# Server Configuration
PORT=3006
NODE_ENV=development

# Pusher Configuration (ОБЯЗАТЕЛЬНО!)
PUSHER_APP_ID=your_pusher_app_id
PUSHER_KEY=your_pusher_key
PUSHER_SECRET=your_pusher_secret
PUSHER_CLUSTER=eu

# Client Configuration (ОБЯЗАТЕЛЬНО!)
REACT_APP_PUSHER_KEY=your_pusher_key
REACT_APP_PUSHER_CLUSTER=eu
```

**Без этих переменных приложение НЕ ЗАПУСТИТСЯ!**

#### Для production:
Все переменные окружения должны быть настроены в вашем хостинге (Render, Heroku и т.д.)

См. подробную инструкцию в [`DEPLOY.md`](DEPLOY.md)

## 🚀 Деплой на Render

Подробная инструкция по деплою доступна в файле [`DEPLOY.md`](DEPLOY.md)

### Быстрый старт:
1. Настройте переменные окружения в Render Dashboard
2. Подключите GitHub репозиторий
3. Используйте `render.yaml` для автоматического деплоя или настройте вручную
4. Следуйте инструкциям в [`DEPLOY.md`](DEPLOY.md)

## 🎮 Правила игры

Секка - карточная игра с 36 картами + Джокер. Игроки получают по 3 карты и соревнуются в комбинациях.

### Комбинации (по силе):
1. **СЕКА ТУЗОВ** (три туза)
2. **32 очка** (Джокер + Туз + фигура)
3. **31 очко** (3 карты одной масти + Туз)
4. **СЕКА МАСТЕЙ** (30 очков)
5. **22 очка** (Два Лба)
6. **21 очко** (Очко)
7. **20 очков**
8. **11 очков**
9. **10 очков**

### Особые правила:
- **Вара** - при ничьей между двумя игроками
- **Ва-банк** - ставка всех фишек
- **Вскрытие** - сравнение карт в определенных условиях
