# 📦 Настройка Git репозитория

## Инициализация репозитория

Выполните следующие команды для инициализации Git и отправки на GitHub:

```bash
# Инициализация Git репозитория
git init

# Добавление всех файлов (кроме тех, что в .gitignore)
git add .

# Первый коммит
git commit -m "Initial commit: Seka game project"

# Переименование ветки в main
git branch -M main

# Добавление remote репозитория
git remote add origin https://github.com/suninvest777/seka.git

# Отправка на GitHub
git push -u origin main
```

## ⚠️ Важно перед коммитом

### Проверьте, что НЕ коммитятся секретные данные:

```bash
# Проверка на наличие секретов в коде
grep -r "PUSHER_SECRET\|PUSHER_KEY\|PUSHER_APP_ID" --exclude-dir=node_modules --exclude=".git" --exclude="*.md"

# Проверка, что .env файлы не добавлены
git status | grep ".env"
```

### Убедитесь, что .gitignore работает:

```bash
# Проверка игнорируемых файлов
git status --ignored
```

## 📝 Рекомендуемые коммиты

После первого коммита, рекомендуется делать коммиты с понятными сообщениями:

```bash
git add .
git commit -m "Описание изменений"
git push
```

## 🔄 Обновление репозитория

Для обновления репозитория после изменений:

```bash
git add .
git commit -m "Описание изменений"
git push
```

## 🚫 Что НЕ должно быть в репозитории

- ❌ Файлы `.env` с реальными секретами
- ❌ `node_modules/` (добавлены в .gitignore)
- ❌ Файлы с паролями и API ключами
- ❌ Локальные конфигурационные файлы с секретами

## ✅ Что должно быть в репозитории

- ✅ Исходный код
- ✅ `package.json` файлы
- ✅ `.gitignore`
- ✅ `README.md`
- ✅ `DEPLOY.md`
- ✅ `ENV_EXAMPLE.txt` (пример переменных окружения)
- ✅ `render.yaml` (конфигурация для деплоя)

