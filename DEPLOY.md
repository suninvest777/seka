# 🚀 Инструкция по деплою на Render

## ⚠️ ВАЖНО: Production Ready

Проект настроен для production деплоя. **Все секретные данные должны быть настроены через переменные окружения!**

Приложение **не запустится** без правильной настройки переменных окружения.

## Подготовка к деплою

### 1. Настройка переменных окружения (ОБЯЗАТЕЛЬНО!)

Перед деплоем необходимо настроить следующие переменные окружения в Render:

#### Для Backend сервиса (seka-api) - ОБЯЗАТЕЛЬНО:
- `NODE_ENV` = `production` ⚠️ **ОБЯЗАТЕЛЬНО**
- `PORT` = `10000` (или любой другой порт, который назначит Render)
- `PUSHER_APP_ID` = ваш Pusher App ID ⚠️ **ОБЯЗАТЕЛЬНО**
- `PUSHER_KEY` = ваш Pusher Key ⚠️ **ОБЯЗАТЕЛЬНО**
- `PUSHER_SECRET` = ваш Pusher Secret ⚠️ **ОБЯЗАТЕЛЬНО**
- `PUSHER_CLUSTER` = `eu` (или ваш кластер, по умолчанию `eu`)

**Без этих переменных сервер НЕ ЗАПУСТИТСЯ!**

#### Для Frontend сервиса (seka-client) - ОБЯЗАТЕЛЬНО:
- `REACT_APP_PUSHER_KEY` = ваш Pusher Key (тот же, что и для backend) ⚠️ **ОБЯЗАТЕЛЬНО**
- `REACT_APP_PUSHER_CLUSTER` = `eu` (или ваш кластер, по умолчанию `eu`)
- `REACT_APP_API_URL` = URL вашего backend сервиса (например: `https://seka-api.onrender.com`)

**Без REACT_APP_PUSHER_KEY приложение НЕ ЗАПУСТИТСЯ!**

### 2. Деплой через Render Dashboard

#### Backend (Web Service):
1. Создайте новый **Web Service** в Render
2. Подключите ваш GitHub репозиторий
3. Настройки:
   - **Name**: `seka-api`
   - **Environment**: `Node`
   - **Build Command**: `cd server && npm install`
   - **Start Command**: `cd server && npm run prod`
   - **Health Check Path**: `/api/test`
4. Добавьте переменные окружения (см. выше)
5. Нажмите **Create Web Service**

#### Frontend (Static Site):
1. Создайте новый **Static Site** в Render
2. Подключите ваш GitHub репозиторий
3. Настройки:
   - **Name**: `seka-client`
   - **Build Command**: `cd client && npm install && npm run build`
   - **Publish Directory**: `client/build`
4. Добавьте переменные окружения (см. выше)
5. Нажмите **Create Static Site**

### 3. Деплой через render.yaml (автоматический)

Если вы используете `render.yaml`, Render автоматически создаст оба сервиса:

1. В Render Dashboard перейдите в **New** → **Blueprint**
2. Подключите ваш GitHub репозиторий
3. Render автоматически обнаружит `render.yaml` и создаст сервисы
4. Настройте переменные окружения в каждом сервисе (см. раздел 1)

## 🔒 Безопасность

### Важно:
- ✅ **НЕ** коммитьте файлы `.env` в Git
- ✅ **НЕ** публикуйте секретные ключи в коде
- ✅ Используйте переменные окружения для всех секретов
- ✅ Регулярно обновляйте секретные ключи

### Проверка безопасности перед коммитом:
```bash
# Проверьте, что нет секретов в коде
grep -r "PUSHER_SECRET\|PUSHER_KEY\|PUSHER_APP_ID" --exclude-dir=node_modules --exclude=".git"
```

## 📝 После деплоя

1. Проверьте, что backend доступен: `https://your-api-url.onrender.com/api/test`
2. Проверьте, что frontend загружается: `https://your-client-url.onrender.com`
3. Убедитесь, что frontend может подключиться к backend (проверьте CORS настройки)

## 🔧 Troubleshooting

### Backend не запускается:
- Проверьте логи в Render Dashboard
- Убедитесь, что все переменные окружения установлены
- Проверьте, что `PORT` установлен правильно

### Frontend не может подключиться к API:
- Проверьте, что `REACT_APP_API_URL` указывает на правильный URL backend
- Убедитесь, что CORS настроен на backend (должен разрешать запросы с frontend домена)

### Pusher не работает:
- Проверьте, что все Pusher переменные окружения установлены
- Убедитесь, что `PUSHER_KEY` одинаковый для frontend и backend
- Проверьте, что кластер указан правильно

